# WEFT GPU Kernels (Scaffold)

This folder contains CUDA kernel scaffolding for planned integration:

- `tile_score.cu`: fast tile candidate scoring kernel
- `cccl_ops.cu`: CCCL/CUB/Thrust helper entry points

The Python prototype currently executes a software reference path.
